import { RetailDashboard } from '@/components/retail-dashboard';
import { SiteChrome } from '@/components/site-chrome';

export default function RetailPage() {
  return (
    <SiteChrome>
      <RetailDashboard />
    </SiteChrome>
  );
}
